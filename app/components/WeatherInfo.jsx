import React from 'react';

export class WeatherInfo extends React.Component {
    constructor (props) {
        super(props);
    }
    render () {
        return (
            <div>
                Weather info here
            </div>
        );
    }
}

export default WeatherInfo;
